<%= form_with(model: hierarchy, local: true) do |form| %>
  <% if hierarchy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hierarchy.errors.count, "error") %> prohibited this hierarchy from being saved:</h2>

      <ul>
      <% hierarchy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="field">
    <%= form.label :parent_id %>
    <%= form.collection_select :parent_id, Hierarchy.order(:name), :id, :name, :include_blank => true %>
  </div>
  <div class="field">
    <%= form.label :children_id %>
    <%= form.collection_select :children_id, Hierarchy.order(:name), :id, :name, :include_blank => true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>